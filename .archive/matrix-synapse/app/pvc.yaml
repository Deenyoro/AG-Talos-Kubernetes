apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synapse-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  # storageClassName: standard
