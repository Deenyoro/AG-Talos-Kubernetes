---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - pvc.yaml
  - configmap.yaml
  - helmrelease.yaml
generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
    - name: meshcentral-oidc-secret
      namespace: default
      files:
        - meshcentral-oidc.enc.sops.yaml
