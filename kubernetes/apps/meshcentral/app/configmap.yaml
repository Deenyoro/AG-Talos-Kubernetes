apiVersion: v1
kind: ConfigMap
metadata:
  name: meshcentral-config
data:
  config.json: |
    {
      "settings": {
        "Cert": "${HOSTNAME}",
        "_WANonly": true,
        "_LANonly": true,
        "Minify": 1,
        "Port": ${PORT},
        "TLSOffload": true,
        "AgentPort": ${PORT},
        "AgentAliasPort": ${PORT},
        "AgentPong": true,
        "MpsPort": ${PORT},
        "MpsAliasPort": ${PORT}
      },
      "domains": {
        "": {
          "Title": "MeshCentral",
          "newAccounts": false,
          "certUrl": "https://${HOSTNAME}/",
          "userQuota": 1048576,
          "meshQuota": 248576
        }
      }
    }
