apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ytdl-material-media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: nfs-client
