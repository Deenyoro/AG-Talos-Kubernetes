---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: invoiceninja-db-backup
  namespace: ${APP_NS}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
#  storageClassName: 
