apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: invoiceninja
spec:
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  storageClassName: cephfs
