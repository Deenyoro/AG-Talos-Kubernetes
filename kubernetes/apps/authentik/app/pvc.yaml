apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
  storageClassName: cephfs
