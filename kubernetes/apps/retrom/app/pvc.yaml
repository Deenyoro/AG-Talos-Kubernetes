---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: retrom-game-data-v1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
  storageClassName: cephfs-media # Due to Retain policy
