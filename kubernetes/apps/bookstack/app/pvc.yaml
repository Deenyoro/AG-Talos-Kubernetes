apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookstack
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: cephfs
