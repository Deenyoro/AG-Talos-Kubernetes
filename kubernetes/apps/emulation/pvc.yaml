apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: emulation-pvc-v1
spec:
  resources:
    requests:
      storage: 30Ti
  accessModes:
    - ReadWriteMany
  storageClassName: cephfs-media-ec
